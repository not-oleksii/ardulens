{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main ArduLens window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
